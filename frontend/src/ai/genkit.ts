// Simplified AI configuration for production builds
// Genkit AI integration disabled to avoid build warnings
export const ai = {
  // Placeholder for AI functionality
  // Can be re-enabled when Genkit dependencies are stable
};
